# Base URL of the Spring Boot backend (vet4kback). The app appends /vet4k internally.
# Change this to point at your deployed backend without touching the code.
VITE_API_BASE_URL=http://localhost:8080
